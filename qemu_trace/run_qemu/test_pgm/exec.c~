#include <stdio.h>
#include <unistd.h>

int main(){
	char buff[1000];
	sprintf(buff, "cat /proc/%d/status", getpid());
	system(buff);
	execlp("sort", "sort", "100M.txt", "-o", "sort_100M.txt", NULL);
}
